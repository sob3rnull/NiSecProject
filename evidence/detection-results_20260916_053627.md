# Detection measurement results

Generated by `scripts/measure-detection.sh` on 2026-09-16 05:41:30.

| Test | Result | Rule fired | Time to alert | Detail |
|---|---|---|---|---|
| _Baseline (idle, no attack)_ | 4 alerts in 120s | - | - | ~120/hour noise floor |
| 1. Port scan (nmap -sS) | DETECTED | 100102 | 7 s | Suricata: medium severity alert - ET SCAN Suspicious inbound to mySQL port 3306 |
| 2. SSH brute-force (hydra) | DETECTED | 5710 | 7 s | sshd: Attempt to login using a non-existent user |
| 3. Ping flood (hping3) | DETECTED | 100120 | 6 s | NISec custom Suricata signature fired - NISEC ICMP ping flood detected (possible DoS) |
| 4. Malware / FIM (EICAR + mass modify) | DETECTED | 100200 | 5 s | NISec: new file created in monitored directory - possible malware drop |

Idle baseline: 4 alerts in 120s (~120/hour). Any attack row below should be read against this floor.

## Sensor performance under flood

```
before: capture.kernel_packets=1439 capture.kernel_packets=1444
after : capture.kernel_packets=1439 capture.kernel_packets=1444
```

`capture.kernel_drops` is the count of packets the kernel discarded before
Suricata could inspect them. Every dropped packet is traffic the IDS never
saw, so a non-zero delta here is a measured blind spot, not a rounding error.
Report the drop rate as (drops delta) / (packets delta).

## Method

- All timestamps are read from the **Wazuh manager's** clock: `T0` immediately
  before launching the attack, alert time from the manager's `alerts.json`.
  One clock means VM skew cannot contaminate the latency figure.
- Only alerts at or after `T0` are counted, so no stale alert can be credited.
- Latency is **end-to-end for the scenario as run** and includes any deliberate
  sleeps inside the attack script (notably the FIM test, which waits for a
  baseline before modifying). It is not manager processing time.
- Resolution is 1 second — the granularity of the Wazuh alert timestamp.
- `NOT DETECTED` means no matching alert inside the stated timeout. Keep those
  rows and explain them; a documented miss is a finding.
